[workspace]
members = ["kernel","bootloader", "common/bootinfo", "common/elf_headers", "tools/mem", "common/acpi_tables", "common/pci", "utils"]
resolver = "3"


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
