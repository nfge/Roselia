[workspace]
members = ["kernel","bootloader", "common/bootinfo", "tools/mem", "common/acpi", "common/pci", "utils", "kernel_api"]
resolver = "3"


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
