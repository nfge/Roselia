// use uefi::boot::ScopedProtocol;
// use uefi::proto::media::fs::SimpleFileSystem;
// use uefi::{CStr16};
// use uefi_raw::{Status};

// pub fn load_elf(elf_name: &CStr16, filesys: ScopedProtocol<SimpleFileSystem>) -> Status {
    
//     Status::SUCCESS
// }